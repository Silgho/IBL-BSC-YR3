# List are written in [] blankets.
# print(len(list)) it will print the length of the list.
#X.reverse() reverses list elements in a list variable.
# X.extend() extends list by adding elements.
#X.remove() removes elements in a list.
#X.pop() removes elements from a list but the removed can be retrieved.
#X.clear() removes elements from a list permanently.
'''
my_str.count() returns length of the string.
my_str.lower() converts to lower case.
my_str.upper() converts to upper case.
my_str.find(':') returns index of first occurrence of the element.
my_str.partition(':') breaks the string into tuple for the element passed.
my_str.split(':') split the string for the parameter passed.
my_str.replace(' ', ' ') replace string with new passed parameters.

'''
